<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>table</title>
    <style>
      
      th, td {
  padding: 5px;
  text-align: left;
  
  border-bottom: 1px solid #ddd;

}
           table{
            width: 50%;
            text-align: center;
            margin-left: 50px;
            margin-top: 50px;
           }
    </style>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

</head>

<body>
  
   
   <table >
      <tr>
        <th>sl.no</th>
        <th>Name</th>
        <th>Email</th>
        <th>status</th>
        <th>privilege</th>
        <th>Delete</th>
        <th>block</th>
        <th>unblock</th>
      </tr>
      <%  i=1 %>
      <% data.reverse().forEach(function(person){ %>           
        <tr>
            
            <td> <%= i++  %></td>          
            <td><%= person.name %></td>             
            <td><%= person.email %></td>  
            <td><%= person.status %></td>
            <td><%= person.privilege %></td>            
            <td><a href="/delete/<%= person._id%>" onclick="return confirm('do you want to delete')">delete</a></td> 
            <td><a href="/block/<%= person._id%>" onclick="return confirm('do you want to block this user')">block</a></td>
            <td><a href="/unblock/<%= person._id%>" onclick="return confirm('do you want to unblock this user')">unblock</a></td>    
    
        </tr>            
        <% }); %>
      </tr>
   </table>
   </div>
<script>
    // pop = (deleted) =>{
    //   if( confirm('Do you want to delete'))
    //   {
    //     alert('yes') 
    
    //      var deleted = yes;
    //   }else{
    //    alert('no')
    //  }
    //  }


</script>
</body>
</html>